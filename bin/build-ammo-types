#!/bin/bash
source "$(dirname "$0")/make-json"

listAllAmmoTypes () {
  grep -roP '"ammoType": "\K[^"]+' ./src/modules | cut -d ':' -f 2-
}

echo -n "building ammo types... "
makeJSON listAllAmmoTypes > ./src/consts/ammo-types.json
echo done

